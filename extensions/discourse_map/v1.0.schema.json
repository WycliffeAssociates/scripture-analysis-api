{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "discourse_map/v1.0",
  "title": "Discourse Map",
  "description": "Structured output of the global discourse pass for a chapter. Anchored at chapter level. Establishes the discourse framework — QUDs, argument structure, boundaries, relational dynamics — that grounds the verse-level analyses.",
  "type": "object",
  "required": ["type", "version", "model", "resources", "argument_structure", "genre_notes"],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "const": "discourse_map",
      "description": "Observation type identifier."
    },
    "version": {
      "type": "string",
      "const": "1.0",
      "description": "Schema version."
    },
    "model": {
      "type": "string",
      "description": "Model that produced this observation. Set by the pipeline after generation, not by the LLM itself."
    },
    "resources": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Resources used to produce this observation, e.g. [\"interpresure\", \"macula\", \"bart_displays\"]. Empty list for zero-shot runs."
    },
    "dominant_quds": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "The dominant Questions Under Discussion active across this chapter."
    },
    "argument_structure": {
      "type": "string",
      "minLength": 1,
      "description": "Prose description of the argumentative, narrative, or poetic arc of the chapter."
    },
    "discourse_boundaries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["verse_start", "verse_end", "description"],
        "additionalProperties": false,
        "properties": {
          "verse_start": {
            "type": "integer",
            "minimum": 1,
            "description": "First verse of this discourse unit."
          },
          "verse_end": {
            "type": "integer",
            "minimum": 1,
            "description": "Last verse of this discourse unit."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "What this discourse unit is doing — its function in the chapter's argument or narrative."
          }
        }
      },
      "description": "Discourse units with verse ranges and functional descriptions."
    },
    "relational_dynamics": {
      "type": "string",
      "description": "The social and relational context operative across the chapter."
    },
    "active_scales": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Scalar dimensions active in this chapter."
    },
    "key_presuppositions": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Background assumptions the chapter takes for granted."
    },
    "genre_notes": {
      "type": "string",
      "minLength": 1,
      "description": "Genre and register observations affecting interpretation."
    }
  }
}
